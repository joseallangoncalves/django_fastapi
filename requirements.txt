# ==============================================================================
#                  REQUISITOS E DEPENDÊNCIAS CONSOLIDADAS
# ==============================================================================

# --- CAMADA BACKEND: MOTOR DE APIS E IA (FastAPI) ---
fastapi[standard]>=0.136.1
uvicorn>=0.47.0
groq>=1.2.0
sqlalchemy>=2.0.0
pyjwt>=2.8.0
python-dotenv>=1.0.1
bcrypt>=4.0.0  # Criptografia nativa direta (substitui passlib/compatibilidade)

# --- CAMADA FRONTEND: PORTAL WEB (Django BFF) ---
django>=5.0.0
httpx>=0.27.0  # Cliente HTTP para comunicação Server-to-Server BFF
